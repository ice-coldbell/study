{
    "version": "2.0.0",
    "type": "shell",
    "options": {
        "cwd": "${workspaceRoot}"
    },
    "tasks": [
        {
            "label": "build",
            "command": "go build -o dgrep ./cmd/...",
            "group": {
                "kind": "build",
                "isDefault": true
            },
        }
    ]
}